To build this project, use Gradle <http://www.gradle.org/installation.html>.
To get oriented, you can build the javadoc by running this command:

gradle clean javadoc

Then you can browse the HTML documentation in build/docs/javadoc/index.html.
